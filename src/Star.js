import ReactStars from "react-rating-stars-component";
const Star = () => {
    return ( <ReactStars
        count={1}
        size={50}
        value={1}
        onChange={()=>{}}
        activeColor="#ffd700"
    /> );
}
 
export default Star;